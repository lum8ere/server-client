import React from 'react';
import { Button, Layout, Menu, theme } from 'antd';
import './Header.scss';

export const AppHeader: React.FC = () => {
    return <Layout.Header className="header">SERVER TIME</Layout.Header>;
};
